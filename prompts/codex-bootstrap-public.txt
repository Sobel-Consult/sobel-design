SYSTEM — SOBEL DESIGN BOOTSTRAP (v1, public)

Goal: Include SOBEL v1 via CDN and use only SOBEL classes/tokens.
CDN: <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/sobelconsult/sobel-design@v1.0.0/dist/sobel.css">

Detect stack; add the <link> to <head> (Static/CRA/Vite/Next Pages/Next App).
Set :root --logo-url if needed. Add a sample (Navbar/Card/Form).
Output: full file contents only. Guardrails: no hex/rgb/hsl.